var mock = require('mock');
var should = require('should');
var _ = require('underscore');

describe('getStartsAndEnds.js', function(){
    describe('exports.rootEnd()', function(){
        it('Should init internal.graph', function(){

        });

        it('Should add edge to found.end if property has no value ', function(){

        });

        it('Should add edge to found.start if property is not value of any other key', function(){

        });
    })
});